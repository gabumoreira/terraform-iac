# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: TerraformInstaller@0
  inputs:
    terraformVersion: 'latest'
- task: TerraformTaskV3@3
  inputs:
    provider: 'aws'
    command: 'init'
    workingDirectory: '$(System.DefaultWorkingDirectory)/terraform/env/dev'
    backendServiceAWS: 'terraform-app-us-east-1'
    backendAWSBucketName: 'terraform-bucket-mack-us-east-1'
    backendAWSKey: 'tf/terraform.ftstate'
- task: TerraformTaskV3@3
  inputs:
    provider: 'aws'
    command: 'plan'
    workingDirectory: '$(System.DefaultWorkingDirectory)/terraform/env/dev'
    environmentServiceNameAWS: 'terraform-app-us-east-1'
- task: TerraformTaskV3@3
  inputs:
    provider: 'aws'
    command: 'apply'
    workingDirectory: '$(System.DefaultWorkingDirectory)/terraform/env/dev'
    environmentServiceNameAWS: 'terraform-app-us-east-1'